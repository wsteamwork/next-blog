import React from 'react';
import {NextComponentType} from 'next';

const Footer: NextComponentType<any> = (props) => {
  return (
    <div>
        <h1>Footer</h1>
    </div>
  );
};

export default Footer;
